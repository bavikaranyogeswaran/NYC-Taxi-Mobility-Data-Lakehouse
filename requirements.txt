# ─────────────────────────────────────────────────────
# NYC Taxi Mobility Data Lakehouse - Python Dependencies
# Python 3.13 compatible versions
# ─────────────────────────────────────────────────────

# Core Compute Engine
pyspark==3.5.4

# Delta Lake integration for PySpark (must match PySpark version)
delta-spark==3.3.0

# HTTP requests for downloading raw data files
requests==2.32.3

# Fast columnar in-memory format for Parquet read/write
# 18.1.0 is the first release with official Python 3.13 wheels
pyarrow==18.1.0

# In-process analytical SQL engine (used for Gold validation)
# 1.2.2 is stable with official Python 3.13 wheels on Windows
duckdb==1.2.2

# Utility: pretty printing of DataFrames during validation
tabulate==0.9.0

# Utility: environment variable management
python-dotenv==1.0.1

# API layer (FastAPI backend in api/)
fastapi==0.115.0
uvicorn==0.30.6
psycopg2-binary==2.9.10

# Structured logging
structlog==24.4.0

# Prometheus metrics (pipeline Pushgateway)
prometheus-client==0.20.0

# Testing
pytest==8.3.5
